version: "3.8"

services:
  backend:
    build:
      context: ./backend
    command: gunicorn gym_site.wsgi --bind 0.0.0.0:8000
    ports:
      - 8000:8000
    depends_on:
      - database
    container_name: django_backend
  
  database:
    image: postgres
    volumes:
      - ./data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    container_name: postgres_db
  
  frontend:
    build:
      context: ./frontend
    volumes:
        - /usr/share/nginx/angular2
    depends_on:
      - backend
    container_name: angular2
  
  nginx:
    image: nginx:latest
    ports:
      - 80:8080
    volumes:
      - ./nginx/nginx-setup.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
      - frontend 
    container_name: nginx


  
