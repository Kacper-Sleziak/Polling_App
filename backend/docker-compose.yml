version: "3.8"

services:
  app:
    build: .
    ports:
      - 8000:8000
    image: polling-app-backend
    command: /bin/sh /django/start-backend.sh
    depends_on:
      - db
    links:
      - db:db
    networks:
      - djangonetwork
    volumes:
      - ./:/django
      - /django/venv

  db:
    image: postgres
    volumes:
<<<<<<< HEAD
      - ./data/db:/var/lib/postgresql/data
    env_file:
      - backend/.env
=======
      - ../data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
>>>>>>> 3e077cb (Small changes to docker-compose)
    container_name: postgresql
    networks:
      - djangonetwork
      
networks:
  djangonetwork:
    driver: bridge
  
